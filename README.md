## Hướng dẫn cài đặt thư viện

Để chạy được mã nguồn trong file `main.py`, cần cài đặt một số thư viện.

Hãy mở Terminal hoặc Command Prompt, sau đó chạy câu lệnh sau:

```bash
pip install requests folium geopy python-dotenv
